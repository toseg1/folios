# Pinned exact versions. Update deliberately, one PR at a time.
# Runtime
typer==0.12.5
click==8.1.7  # typer 0.12.x breaks on click>=8.2 (Parameter.make_metavar signature change)
rich==13.9.4
pydantic==2.10.6  # dash (pulled in by stockdex) needs >=2.10
psycopg[binary]==3.2.13
PyYAML==6.0.2
requests==2.32.3
tenacity==9.0.0
python-dotenv==1.0.1
yfinance==1.7.0
curl_cffi==0.16.3  # yfinance needs >=0.15; also covers stockdex, see below

# stockdex's own transitive deps, listed explicitly instead of letting
# `pip install stockdex` resolve them: its metadata pins curl_cffi==0.12.0
# exactly, which conflicts with yfinance's >=0.15 (verified working fine
# against 0.16.3 in practice — the pin is just stale). dash/plotly/selenium
# are pulled in because stockdex imports them eagerly at module load even
# though we only use its justetf_* methods.
beautifulsoup4==4.12.3
lxml==6.1.3
peewee==4.5.1
platformdirs==4.11.9
protobuf==7.36.1
dash==4.4.1
plotly==7.1.0
selenium==4.49.0
# stockdex itself is NOT listed here — it must be installed separately
# with --no-deps (`make setup` / ci.yml do this already), because even
# with every transitive dep above already satisfied, a plain
# `pip install stockdex` on this same requirements.txt would still
# re-resolve its own curl_cffi==0.12.0 pin and hit the same conflict.

google-auth==2.58.0
google-auth-oauthlib==1.4.1
google-api-python-client==2.200.0

# Dev / test
ruff==0.7.4
pytest==8.3.3
pytest-cov==5.0.0

# The package itself, editable
-e .
